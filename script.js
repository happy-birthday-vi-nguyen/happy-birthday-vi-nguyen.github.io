// Minimal script for invitation page
document.addEventListener('DOMContentLoaded', () => {
  // Placeholder: no interactive behavior required for static invitation.
  // If you want click-to-open or confetti, tell me and I can add it back.
  console.log('Invitation loaded');
});
